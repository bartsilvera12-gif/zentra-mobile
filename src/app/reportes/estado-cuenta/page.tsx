import { ReportePlaceholder } from "@/components/reportes/ReportePlaceholder";

export default function ReporteEstadoCuentaPage() {
  return (
    <ReportePlaceholder
      eyebrow="Reportes · Finanzas"
      title="Estado de cuenta empresa"
      subtitle="Resumen financiero general: saldos, movimientos, ventas, compras, gastos, cobros y pagos del período."
    />
  );
}
